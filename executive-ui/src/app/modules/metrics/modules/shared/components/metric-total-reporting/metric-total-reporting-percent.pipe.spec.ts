import { TotalReportingPercentPipe } from './metric-total-reporting-percent.pipe';

describe('TotalReportingPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalReportingPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
