import { BuildingBlockMetricNamePipe } from './building-block-metric-name.pipe';

describe('BuildingBlockMetricNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BuildingBlockMetricNamePipe();
    expect(pipe).toBeTruthy();
  });
});
