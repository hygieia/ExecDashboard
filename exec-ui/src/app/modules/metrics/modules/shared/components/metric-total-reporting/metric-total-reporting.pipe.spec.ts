import { TotalReportingPipe } from './metric-total-reporting.pipe';

describe('TotalReportingPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalReportingPipe();
    expect(pipe).toBeTruthy();
  });
});
